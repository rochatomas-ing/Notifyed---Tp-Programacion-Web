version: "2"
sql:
  - engine: "postgresql" # o "mysql", "sqlite"
    queries: "./db/queries/"
    schema: "./db/schema/"
    gen:
      go:
        package: "db"
        out: "./db/sqlc/"
        emit_json_tags: true